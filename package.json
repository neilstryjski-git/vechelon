{
  "name": "vechelon-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "admin"
  ],
  "scripts": {
    "build": "npm run build --prefix admin && rm -rf public && mkdir -p public/portal && cp -r admin/dist/. public/portal/ && cp index.html prototype.html echelon-logo-v10.html public/ && cp -r admin/public/. public/portal/ || true"
  }
}
