{
  "name": "vechelon-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "admin"
  ],
  "scripts": {
    "build": "npm run build --prefix admin && rm -rf dist_production && mkdir -p dist_production/portal && cp -r admin/dist/. dist_production/portal/ && cp index.html prototype.html echelon-logo-v10.html dist_production/ && cp -r admin/public/. dist_production/portal/ || true"
  }
}
