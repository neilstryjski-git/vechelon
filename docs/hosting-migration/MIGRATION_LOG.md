# Vercel → Cloudflare migration log

Single log for all affected repos (`vechelon`, `neil-branding`, `itin-wizard`), per the brief. Every change records what, where, when, why. Stride goal G35.

| When (UTC) | Phase | What | Where | Why |
|---|---|---|---|---|
| 2026-09-22 | 1 | Inventory produced: `docs/hosting-migration/inventory.md`. No infrastructure changed. | vechelon repo, branch W294 | Brief Phase 1 gate — surprises on paper before production. |
| 2026-09-22 | 1 | Read-only probes: NS/records via Google + Cloudflare DoH; HTTPS headers per host; Vercel REST (projects, env names, domains, aliases, tokens, webhooks, drains); Supabase Management API auth config (prod + staging); Supabase secrets names; repo greps. | external APIs, three local repos | Facts for the inventory. No values or tokens recorded. |
| 2026-09-22 | 1 | Review round 1 corrections: `/privacy` rewrite added from `master:vercel.json` (explorer had read the `rail3-integration` copy); `vechelon.ca` apex re-recorded as a flattened alias, not static A; cutover-record Port row added. | inventory.md | Reviewer findings. |
| 2026-09-22 | 1 | Review round 2 (cap): fixes verified; cutover-record count corrected to nine (post-cap minor, no further round); criterion 2 (Porkbun export) remains a recorded gap. | inventory.md | Reviewer findings. |
| 2026-09-22 | 1 | Decisions recorded: S1 blog.productdelivered.ca dropped from scope (never existed); S9 public Cloudflare previews accepted. | inventory.md | Neil, in review of the inventory. |
| 2026-09-22 | 1 | Decisions recorded: S7 add Google SPF/DKIM/DMARC to productdelivered.ca at Cloudflare (logged addition, Phase 2); S5 wildcard *.vechelon.ca (Phase 3). | inventory.md | Neil. |
| 2026-09-22 | 1 | Authoritative zone exports pulled from the Porkbun API and committed (`dns-export-*.json`). Diff vs probes: +1 Google verification CNAME on productdelivered.ca; vechelon.ca apex = ALIAS confirmed. Acceptance criterion 2 of W294 now satisfied. | docs/hosting-migration/ | Phase 2 source of truth. |
| 2026-09-22 | 1 | Play-verification hold on vechelon.productdelivered.ca cleared by Neil; Phase 4 cuts all hosts together. | inventory.md | Neil. |
| 2026-09-22 | 1 | Cloudflare account created by Neil (neil@productdelivered.ca, id aa40d158e471dc915ede763a273688cd); scoped API token created and verified (`/user/tokens/verify` = active). Zero zones. | Cloudflare | Phase 2 prerequisite. |
| 2026-09-22 | 1 | Google DKIM key generated by Neil (Google Admin); planned Phase 2 additions for productdelivered.ca (SPF, DKIM, DMARC) recorded in `dns-additions-productdelivered.ca.json`. Not published yet. | docs/hosting-migration/ | Decision S7. |
| 2026-09-22 | 1 | Google Maps key referrer list captured (4 entries; one is the Vercel staging host). No change made. | inventory.md §3 | Phase 3 input. |
| 2026-09-22 | 2 | W295 claimed. Fresh Porkbun pull diffed against committed exports: identical (11 + 13 records). | Porkbun API | Copy from a fresh export, not a snapshot. |
| 2026-09-22 | 2 | Cloudflare zones created (Free plan, type full): productdelivered.ca `b60f1e73db57de27018b507ae83e438b`, vechelon.ca `79cf289addb224ac5330f5d73f8264ae`. Assigned nameservers: delilah.ns.cloudflare.com, nolan.ns.cloudflare.com. Status pending until NS change. | Cloudflare API | Brief Phase 2. |
| 2026-09-22 | 2 | Records created from the exports (NS rows skipped; vechelon.ca apex ALIAS → CNAME with Cloudflare root flattening). All DNS-only (grey cloud). Cutover records (productdelivered.ca @ A, www, vechelon, itin-wizard; vechelon.ca @, admin, 3 clubs) set to TTL 60; all other TTLs kept as exported (apex MX 3600 / verification TXT 600 restored after an over-broad TTL rule caught them). | Cloudflare API | Brief: lower TTLs on records that change at cutover; never proxy Vercel. |
| 2026-09-22 | 2 | ADDITIONS on productdelivered.ca (decision S7): TXT SPF `v=spf1 include:_spf.google.com ~all`, TXT `google._domainkey` (Google DKIM 2048), TXT `_dmarc` `p=none; rua=mailto:neil@productdelivered.ca`. Not part of the verbatim copy. | Cloudflare API | Neil's decision 2026-09-22. |
| 2026-09-22 | 2 | Verification before NS change: Cloudflare record list diffed against exports — vechelon.ca 9/9 exact, productdelivered.ca 7/7 exact + 3 additions. Answer-by-answer query of curitiba.ns.porkbun.com vs delilah.ns.cloudflare.com for all 18 names: identical except the 3 additions. Nothing proxied. | dnsq.py (raw UDP) | Gate for the nameserver change. |
| 2026-09-23 | 2 | Nameservers changed at Porkbun by Neil (both domains → delilah/nolan.ns.cloudflare.com). Porkbun API `getNs` confirmed the setting; .ca registry (any.ca-servers.ca) published the new delegation ~20 min later. | Porkbun panel | Brief Phase 2. |
| 2026-09-23 | 2 | Post-flip verification from outside: Google + 1.1.1.1 both return Cloudflare NS for both domains; all 11 A/CNAME/MX answers identical to the export (BAD COUNT 0); all 7 TXT records (6 names) present incl. the 3 additions; apex vechelon.ca flattens to Vercel IPs; postflip.sh probed 5 hostnames (all 200, `server: Vercel`) and the reviewer independently confirmed all 9 hostnames are served by Vercel (7 × 200; both apexes 307/308 redirects, as in inventory S4). productdelivered.ca zone active at Cloudflare; vechelon.ca activation check triggered. | postflip.sh | Users notice nothing. |
| 2026-09-23 | 2 | Google Admin "Start authentication" pressed by Neil for productdelivered.ca (DKIM signing on). | Google Admin | Decision S7. |
| 2026-09-23 | 2 | Email verification in received Gmail headers: from neil@productdelivered.ca — SPF PASS (209.85.220.41), DKIM PASS (productdelivered.ca), DMARC PASS. Magic-link mail from racer-sportif.vechelon.ca via Resend — SPF PASS (54.240.9.10), DKIM PASS (vechelon.ca), DMARC PASS. | Gmail "Show original" | Brief Phase 2 exit check; first-ever authentication of company mail. |
| 2026-09-23 | 3 | W296 claimed. Product choice recorded: **Cloudflare Workers with static assets, one Worker per site** (not Pages) — unlimited static bandwidth, no commercial-use restriction, wildcard hostnames via a Worker route (Pages has no wildcard custom domains), and the neil-branding redesign already targets it. Free plan. | — | Brief Phase 3. |
| 2026-09-23 | 3 | Account `workers.dev` subdomain created: `productdelivered` (account had none; hard prerequisite for any preview URL). | Cloudflare API | — |
| 2026-09-23 | 3 | Marketing site (neil-branding `master`, lift-and-shift) deployed as Worker `productdelivered-site` → https://productdelivered-site.productdelivered.workers.dev. Config on branch `cloudflare` of neil-branding: wrangler.jsonc (assets dir `.`, `html_handling: none` to keep Vercel's exact-path behaviour), `_redirects` (`/` → index.html, `/ptap`, `/jack-hanger` ported as 200 rewrites), `.assetsignore` excluding non-site files (ProductDocuments, pillar-docs, docs, *.md, *.mjs, *.log, config) — Vercel served some of these from the repo root (e.g. NEW_PROJECT_SETUP.md, dev-server.mjs returned 200); tightened deliberately. Verified byte-identical to Vercel on /, /vechelon.html, /ptap, /jack-hanger; `/vechelon` 404 on both. The Astro `redesign` branch replaces this Worker when Neil signs it off. | neil-branding repo, Cloudflare | Marketing site first, proves the pipeline. |
| 2026-09-23 | 3 | Vechelon web app: `wrangler.jsonc` + `worker/index.ts` (host/path router reproducing every vercel.json rule; filesystem-first; `/portal/assets/*` excluded from the Worker), `wrangler` devDependency, `static/.well-known/assetlinks.json` (`[]`) copied by the build. Deployed as Worker `vechelon-web` → https://vechelon-web.productdelivered.workers.dev. Verified: /portal/ and deep links 200, /admin/* 308 → /portal/*, /prototype /roadmap /privacy 200, /.well-known/assetlinks.json 200 application/json no redirect, hashed bundle 200 with CF-Cache-Status. | vechelon repo branch W296, Cloudflare | Port column. |
| 2026-09-23 | 3 | **Preview host namespace** (needed because the app resolves club/admin/legacy shape from the exact hostname; a bare workers.dev host renders ClubNotFound): `<name>.preview.vechelon.ca` ≡ `<name>.vechelon.ca`, `legacy.preview.vechelon.ca` ≡ vechelon.productdelivered.ca, and the same rule for `.staging.`. Implemented in admin/src/lib/extractSlug.ts (`effectiveHostname`, `hostNamespace`) + portalBase.ts (legacy base, ride links stay in-namespace) and mirrored in the Worker. Custom domains created on the Worker: racer-sportif.preview / admin.preview / legacy.preview .vechelon.ca (Cloudflare added proxied AAAA 100:: records + per-host certificates). | vechelon repo, Cloudflare DNS (vechelon.ca zone) | Test the production artifact on real hostnames. |
| 2026-09-23 | 3 | Staging Race Control re-homed: Worker `vechelon-web-staging` (`--env staging`), built with the staging Supabase URL + publishable key (from mobile/eas.json), custom domain **racer-sportif.staging.vechelon.ca** (replaces vechelon-rail3-staging.vercel.app). Also at https://vechelon-web-staging.productdelivered.workers.dev. | Cloudflare | Port column (S2). |
| 2026-09-23 | 3 | Supabase auth allow-list update (prod: add `https://*.preview.vechelon.ca/**`; staging: site_url → `https://racer-sportif.staging.vechelon.ca`, add `https://*.staging.vechelon.ca/**`) — applied via Management API after Neil's explicit approval (2026-09-23): prod uri_allow_list now also `https://*.preview.vechelon.ca/**`, `https://legacy.preview.vechelon.ca/portal/**`; staging site_url `https://racer-sportif.staging.vechelon.ca`, allow-list adds `https://*.staging.vechelon.ca/**` (old vercel.app entry kept until Phase 5). Production entries untouched. | Supabase | External config. |
| 2026-09-23 | 3 | Certificates issued (Google Trust Services, per-host advanced certs) for the 3 preview hosts + the staging host. Verified over HTTPS: club/admin/staging shapes serve the SPA shell for / and deep links; legacy.preview.vechelon.ca serves landing.html at / (byte-identical to production), 307s /ride/* → /portal/ride/*, 404s /rides — exactly the production host's behaviour; /admin/* 308s everywhere; assetlinks.json 200 JSON everywhere. Preview bundle targets prod Supabase, staging bundle targets staging Supabase. | `docs/hosting-migration/preview_verify_2026-09-23.txt` (curl matrix transcript) | Gate for UAT. |
| 2026-09-23 | 3 | UAT by Neil on the preview hostnames (club, admin, legacy) and the staging Race Control host against the live Supabase backends: reported OK, nothing different from production. | previews | Brief Phase 3 exit check. |
| 2026-09-23 | 3 | Push-to-deploy pipeline: GitHub Actions instead of Cloudflare Workers Builds (the dashboard OAuth step needs Neil's GitHub login; Actions already existed in the repo). `.github/workflows/deploy-cloudflare.yml` in vechelon (push to master → build with VITE_* secrets → empty-portal + missing-secrets guards → `wrangler deploy`) and in neil-branding (branch `cloudflare`, then master). Secrets set via `gh secret set` after Neil's explicit approval: CLOUDFLARE_API_TOKEN (both repos), VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY / VITE_GOOGLE_MAPS_API_KEY (vechelon). neil-branding run #35810216728 succeeded end to end. The vechelon workflow cannot be dispatched until it exists on master (GitHub rule), so its first run is the W296 merge itself — which deploys to the preview Worker only (production hostnames are attached in Phase 4). | GitHub | Replaces Vercel's git integration. |
| 2026-09-23 | 3 | itin-wizard deployed as Worker `itin-wizard` → https://itin-wizard.productdelivered.workers.dev from a clean checkout of `main` (branch `cloudflare`: wrangler.jsonc with `not_found_handling: single-page-application`, `deploy:cloudflare` npm script). Build values taken from the live public bundle (Vercel API refused decrypt). Its `public/_redirects` catch-all (`/* /index.html 200`) is rejected by Cloudflare as a loop and is dropped at deploy; the SPA fallback replaces it. Same bundle hash as Vercel (`index-VyR14XSS.js`); / and deep routes 200. The uncommitted work in the local itin-wizard checkout was left untouched. Deployment Protection (S9) not ported — public previews accepted. | itin-wizard repo, Cloudflare | Third Vercel project. |
| 2026-09-23 | 3 | Google Maps key referrers extended by Neil (`*.preview.vechelon.ca/*`, `*.staging.vechelon.ca/*`; existing four entries untouched). Verified: Static Maps returns 200 image/png with a preview or staging Referer and with the production Referer; 403 for an unlisted site. | Google Cloud console | External config. |
| 2026-09-23 | 3 | **Deferred to Phase 4, deliberately (port-column items with no Phase 3 action):** (a) apex Redirect Rules `productdelivered.ca → www` (307) and `vechelon.ca → vechelon.productdelivered.ca` (308) — a Cloudflare Redirect Rule only fires on a proxied hostname, and both apexes must stay DNS-only → Vercel until cutover (brief: never proxy Vercel), so the rules are created in the same step that repoints the apexes; (b) the S5 wildcard `*.vechelon.ca` route — attaching it now would capture the preview/staging custom domains (route wildcards span labels) and pull tenant traffic before cutover; created in Phase 4 alongside the club records, with a more-specific no-Worker route for `*.preview.vechelon.ca/*` and `*.staging.vechelon.ca/*` if needed. The "no such club" page already exists (`admin/src/pages/ClubNotFound.tsx`, rendered for any unresolvable slug) — nothing to build. | — | Keeps Phase 4 = record changes + these two rule creations. |
| 2026-09-23 | 3 | Review round 1 fixes: bare `/ride` on the legacy host now 307s to `/portal/ride/` (Vercel's optional `:path*`); `run_worker_first` excludes the PWA/service-worker files, manifest, icons and logos as well as the hashed bundle; unit tests added — `admin/src/lib/extractSlug.test.ts` (namespace cases) and `admin/src/lib/workerRouter.test.ts` (every router rule against a stub ASSETS binding), 26 passing. Production bundle rebuilt and redeployed to the preview Worker; re-verified (`preview_verify_2026-09-23_postfix.txt`): bare `/ride` 307 on the legacy shape matches Vercel; sw.js/registerSW.js/manifest/logo served with CF-Cache-Status (asset store, not the Worker). | vechelon repo | Reviewer findings. |
| 2026-09-23 | 3 | Deploy pipeline fix: `deploy-cloudflare.yml` first run failed ("Wrangler requires at least Node.js v22.0.0. You are using v20"); node-version set to 22 (PR #109); rerun #35812164898 green, preview Worker picked up bundle `index-DPI5iUSF.js`. | vechelon repo | wrangler 4.136 needs Node ≥22. |
| 2026-09-23 23:40Z | 4 | Pre-cutover snapshot of every record in both zones saved as `dns-precutover-productdelivered.ca.json` / `dns-precutover-vechelon.ca.json` (rollback source: each cutover record's Vercel value + TTL 60). Parity check: www `/` and `/ptap`, itin-wizard `/`, legacy `/portal/`, racer-sportif `/` — Vercel vs Worker byte-identical (md5). W297 claimed, branch `W297` off master. | vechelon repo | Rollback values on paper before the first flip (brief Phase 4). |
| 2026-09-23 23:54Z | 4 | **www.productdelivered.ca → Cloudflare.** Deleted DNS-only CNAME `32741a363a8128ad.vercel-dns-017.com` (rollback value; Cloudflare refuses a custom domain while an external CNAME exists — `override_existing_dns_record` does not cover it). Attached as custom domain on Worker `productdelivered-site` (creates proxied AAAA `100::`, advanced cert requested). Verified via Google DoH + `--resolve`: `server: cloudflare`, `/` md5 identical to the Worker, `/ptap` `/jack-hanger` `/index.html` 200, unknown path 404, TLS valid (Universal cert). Neil approved all nine record deletions 2026-09-23. | Cloudflare (zone productdelivered.ca) | Phase 4 cutover, site 1 of 5. |
| 2026-09-23 23:56Z | 4 | **productdelivered.ca apex → Cloudflare.** Single Redirect ruleset `8751a464…` created (rule `apex_to_www`: `http.host eq "productdelivered.ca"` → `https://www.productdelivered.ca` + path, 307, query preserved — Vercel parity measured beforehand: 307, path+query kept). Apex A record patched from `216.198.79.1` DNS-only (rollback value) to originless placeholder `192.0.2.0` proxied. **Always Use HTTPS turned on for both zones** — Vercel 308s http→https on every host and a Worker custom domain served `http://www` as 200 until this was on; no effect on DNS-only records, so vechelon.ca hosts still on Vercel are untouched. | Cloudflare | Phase 4 cutover, apex redirect (deferred from Phase 3). |
| 2026-09-23 23:57Z | 4 | Apex verified after propagation: `https://productdelivered.ca/` 307 → www, `/ptap?x=1` keeps path+query, `http://` apex 307 straight to https www, `http://www` 301 → https (Vercel used 308; both static sites, no POST forms — accepted). MX / SPF / verification TXT unchanged. | — | Verification. |
| 2026-09-23 23:57Z | 4 | **itin-wizard.productdelivered.ca → Cloudflare.** Deleted DNS-only CNAME `cname.vercel-dns.com` (rollback value); attached as custom domain on Worker `itin-wizard`. Verified: `server: cloudflare`, `/` md5 identical to the Worker, SPA fallback on deep paths, bundle `index-VyR14XSS.js` 200. | Cloudflare | Phase 4 cutover, site 2 of 5. |
| 2026-09-23 23:58Z | 4 | **vechelon.productdelivered.ca (legacy host) → Cloudflare.** Deleted DNS-only CNAME `cname.vercel-dns.com` (rollback value); attached as custom domain on Worker `vechelon-web`. Verified: `/portal/` md5 identical to the preview, `/` landing 200, `/ride` 307 `/portal/ride/`, `/ride/abc` 307, `/admin` 308 `/portal/`, `/portal/ride/abc` 200, `/.well-known/assetlinks.json` application/json, manifest served, `/privacy` 200, bundle `index-DPI5iUSF.js` contains the Supabase URL. | Cloudflare | Phase 4 cutover, site 3 of 5. |
| 2026-09-23 23:59Z | 4 | **admin.vechelon.ca → Cloudflare.** Deleted DNS-only CNAME `cname.vercel-dns.com` (rollback value); custom domain on `vechelon-web`. Verified: md5 identical to admin.preview, `/ride/abc` 200 (slug-host deep link), `/admin` 308, `/portal/sw.js` text/javascript. | Cloudflare | Phase 4 cutover, site 4 of 5 (Vechelon hosts). |
| 2026-09-24 00:00Z | 4 | **Wildcard `*.vechelon.ca` (S5).** Worker routes created in order: `*.preview.vechelon.ca/*` → no Worker (`d3f9ee25…`), `*.staging.vechelon.ca/*` → no Worker (`81bf9cb1…`), then `*.vechelon.ca/*` → `vechelon-web` (`e34cc075…`). More-specific routes win, so the preview/staging custom domains keep their own Workers (staging = `vechelon-web-staging`). Wildcard DNS `*.vechelon.ca` AAAA `100::` proxied (`e250cf12…`). Universal cert already covers `*.vechelon.ca` (active). Club CNAMEs to Vercel still take precedence over the wildcard until each is deleted (next rows). Rollback: delete the wildcard record + the three routes. | Cloudflare (zone vechelon.ca) | Deferred from Phase 3; removes per-club DNS/Vercel onboarding. |
| 2026-09-24 00:00Z | 4 | Wildcard verified: `nosuchclub.vechelon.ca` → Worker (`server: cloudflare`, portal index md5 identical → app renders ClubNotFound); `racer-sportif.staging` md5 unchanged (`abc5879b`, still the staging Worker), `racer-sportif.preview` / `admin.preview` / `legacy.preview /ride` unchanged. | — | Verification. |
| 2026-09-24 00:01Z | 4 | **racer-sportif.vechelon.ca → Cloudflare.** Deleted DNS-only CNAME `cname.vercel-dns.com` (rollback value); host now served by the wildcard. Verified: md5 identical to preview, `/ride/abc` 200, `/auth` 200, `/portal/racer-sportif-logo.png` image/png, `/admin` 308. | Cloudflare | Phase 4 cutover, club 1/3. |
| 2026-09-24 00:01Z | 4 | **bikes-and-beers.vechelon.ca → Cloudflare.** Deleted DNS-only CNAME `cname.vercel-dns.com` (rollback value). Verified: md5 identical, `/ride/abc` 200, club logo image/png. | Cloudflare | Club 2/3. |
| 2026-09-24 00:02Z | 4 | **lakeside-wheelers.vechelon.ca → Cloudflare.** Deleted DNS-only CNAME `cname.vercel-dns.com` (rollback value). Verified: md5 identical, `/ride/abc` 200. | Cloudflare | Club 3/3. All nine cutover records now off Vercel except the vechelon.ca apex (next row). |
| 2026-09-24 00:03Z | 4 | **vechelon.ca apex → Cloudflare.** Single Redirect ruleset `ecbbd3c6…` created (rule `apex_to_legacy_host`: `http.host eq "vechelon.ca"` → `https://vechelon.productdelivered.ca` + path, 308, query preserved). Apex record patched in place from flattened CNAME `cname.vercel-dns.com` DNS-only (rollback value) to AAAA `100::` proxied. Verified after propagation: `/` 308, `/portal/ride/abc?y=2` keeps path+query, `http://` 308 straight to https — identical to Vercel's measured behaviour. No MX/TXT on this apex (none existed). | Cloudflare (zone vechelon.ca) | Phase 4 cutover, last record. |
| 2026-09-24 00:04Z | 4 | **Cutover complete — final sweep:** all nine hostnames answer `server: cloudflare` (www 200, apex 307, itin-wizard 200, legacy 200, vechelon.ca 308, admin 200, racer-sportif / bikes-and-beers / lakeside-wheelers 200); preview + staging hosts unchanged. Elapsed 23:54Z → 00:04Z. Vercel projects, domains and deployments untouched (rollback path until Phase 5). | — | Brief Phase 4 exit. |
| 2026-09-24 00:06Z | 4 | Config brought in line with the live routing so the next deploy is idempotent: `wrangler.jsonc` now lists `vechelon.productdelivered.ca` + `admin.vechelon.ca` custom domains and the `*.vechelon.ca/*` route (wrangler's deploy PUTs the Worker's full route list — an unlisted wildcard would be deleted on the next push); neil-branding `cloudflare` branch `50be86d` lists `www.productdelivered.ca`; itin-wizard `cloudflare` branch `ce2ddb5` lists `itin-wizard.productdelivered.ca`. `wrangler deploy --dry-run` clean. `CLAUDE.md`: stack line → Cloudflare Worker + GitHub Actions; incident runbook step 6 `wrangler rollback` replaces `vercel promote`. | vechelon, neil-branding, itin-wizard repos | Source of truth = config. |
| 2026-09-22 | 1 | Stride goal G35 created (W294–W298, one task per phase). | Stride board 116 | Track the work. |

Pending (Phase 4): Neil phone smoke test on the live hosts; W297 review + merge; then the ≥48 h stability window before Phase 5 (W298).
