## before_doing
git fetch origin master --quiet || true
git checkout -b $TASK_IDENTIFIER 2>/dev/null || git checkout $TASK_IDENTIFIER

## after_doing
# Placeholder — add once React/Firebase scaffolding is in place
# e.g. npm test, npm run lint

## before_review
echo "PR creation deferred for $TASK_IDENTIFIER — open with: gh pr create --title \"$TASK_IDENTIFIER: $TASK_TITLE\" --base master"

## after_review
# Vercel deploys automatically on merge — nothing needed here