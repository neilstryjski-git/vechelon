# Vechelon Rail 3 — Expo public env vars.
# EXPO_PUBLIC_* vars are inlined into the client bundle at build time.
# The anon key is a publishable client key (RLS enforces access) — but still
# do NOT commit a real .env file; copy this to .env and fill in values.
#
# Shared Supabase project: drktcxggaizkbvqccfhp (Vechelon)
EXPO_PUBLIC_SUPABASE_URL=https://drktcxggaizkbvqccfhp.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
